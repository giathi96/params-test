/*
    def remote = [:]
    remote.name = 'sil-toolserver-1'
    remote.host = '10.134.134.10'
    remote.user = 'cust'
    remote.password = 'AvayaSIL123$'
    remote.allowAnyHosts = true
*/

pipeline {
    agent any
    stages {
        stage('Configuration') {
            steps{
                echo "STAGE: CONFIGURATION"
                print "Lab: " + params.Lab
                echo "Task: ${Task}"
                echo "Task: ${params['Tenant Index']}"
                echo "Task: ${params['Tenant Features']}"
            }
        }  
    }
}

        